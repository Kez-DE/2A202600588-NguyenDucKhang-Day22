# ══════════════════════════════════════════════════════════════════════════
#  Day 22 Lab — requirements (ĐÃ PIN để resolve sạch)
#
#  LƯU Ý QUAN TRỌNG VỀ PHIÊN BẢN:
#  - ragas 0.4.3 cần langchain-community 0.3.x  →  toàn bộ langchain phải là 0.3.x
#    (langchain 1.x đã xoá langchain_community.chat_models.vertexai → vỡ ragas).
#  - Do đó guardrails-ai phải dùng dòng 0.6.x (0.7.x trở lên yêu cầu
#    langchain-core >= 1.0, xung đột với ragas).
#  - openai phải là 1.x: thoả guardrails 0.6.8 (<2), langchain-openai (>=1.104.2),
#    và instructor 1.10 (<2, dep của ragas).
#
#  Khuyến nghị Python 3.10–3.12 (KHÔNG dùng 3.13/3.14 — faiss/ragas chưa hỗ trợ).
# ══════════════════════════════════════════════════════════════════════════

# ── LangChain core (KHOÁ ở dòng 0.3.x — bắt buộc cho ragas) ───────────────
langchain>=0.3.0,<0.4.0
langchain-core>=0.3.0,<0.4.0
langchain-community>=0.3.0,<0.4.0
langchain-text-splitters>=0.3.0,<0.4.0

# ── LLM Provider chính: OpenAI / OpenRouter ───────────────────────────────
langchain-openai>=0.2.0,<0.4.0

# ── LLM Providers khác (TUỲ CHỌN — chỉ cần nếu đổi PROVIDER) ──────────────
# Đã pin về dòng tương thích langchain-core 0.3.x. Nếu chỉ dùng OpenAI,
# có thể bỏ comment hoặc xoá 3 dòng dưới.
langchain-anthropic>=0.2.0,<0.4.0       # Anthropic Claude
langchain-google-genai>=2.0.0,<3.0.0    # Google Gemini
langchain-ollama>=0.2.0,<0.4.0          # Ollama (local)

# ── LangSmith (tracing + Prompt Hub) ──────────────────────────────────────
langsmith>=0.2.0

# ── Vector store ──────────────────────────────────────────────────────────
faiss-cpu>=1.7.0

# ── Embeddings local (cho OpenRouter — vốn KHÔNG có API embeddings) ────────
fastembed>=0.3.0                 # ONNX, nhẹ, chạy local, miễn phí, không cần key

# ── RAG Evaluation ────────────────────────────────────────────────────────
ragas>=0.4.0,<0.5.0
instructor>=1.6.0,<1.11.0        # dep của ragas — pin để chấp nhận openai 1.x
datasets>=2.0.0                  # required by RAGAS internally
numpy>=1.25.0

# ── Guardrails AI (0.6.x — tương thích langchain-core 0.3.x) ──────────────
guardrails-ai>=0.5.0,<0.7.0

# ── Utilities ─────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
openai>=1.104.2,<2.0.0           # 1.x: thoả guardrails 0.6.8 + langchain-openai + instructor
tiktoken>=0.5.0                  # token counting
